import 'package:flutter/material.dart';
class CustomerOrderItemRow extends StatelessWidget { const CustomerOrderItemRow({super.key}); @override Widget build(BuildContext context) => const SizedBox.shrink(); }
